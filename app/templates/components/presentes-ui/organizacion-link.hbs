{{yield}}
{{#if cargando}}
  Cargando
{{else}}
  {{#if fila}}
    {{#if session.isAuthenticated}}
      {{#link-to 'app.organizaciones.detalle' fila.id}}{{fila.nombre}}{{/link-to}}
    {{else}}
      {{#link-to 'publico.organizaciones.detalle' fila.id}}{{fila.nombre}}{{/link-to}}
    {{/if}}
  {{/if}}
{{/if}}
