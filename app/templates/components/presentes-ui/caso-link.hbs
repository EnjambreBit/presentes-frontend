{{yield}}
{{#if cargando}}
  Cargando
{{else}}
  {{#if fila}}
    {{#if session.isAuthenticated}}
      {{#link-to 'app.casos.detalle' fila.id}}{{fila.nombre}} {{fila.apellido}}{{/link-to}}
    {{else}}
      {{#link-to 'publico.casos.detalle' fila.id}}{{fila.nombreCompleto}}{{/link-to}}
    {{/if}}
  {{/if}}
{{/if}}
