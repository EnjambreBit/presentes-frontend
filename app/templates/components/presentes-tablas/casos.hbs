{{#if session.isAuthenticated}}
  {{presentes-ui/tabla
    modulo="casos"
    columnas=columnas
    tarea=tarea
    tareaFiltros=crearFiltros
  }}
{{else}}
{{presentes-ui/tabla
  modulo="casos"
  columnas=columnas
  tarea=tareaPublico
  tareaFiltros=crearFiltrosPublico
}}
{{/if}}
{{yield}}
